export module lib2.scan:scanner;

namespace lib2
{
    export
    template<class T, class CharT>
    struct scanner
    {
        scanner() = delete;
        scanner(const scanner&) = delete;
    };
}